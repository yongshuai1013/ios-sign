# WISP proxy base URL used by Remote (Wi-Fi) pairing to bridge
# WebSocket -> device TCP (browsers cannot open raw TCP sockets).
# Deploy backend/ (Cloudflare Worker) first, then set e.g.:
# VITE_WISP_URL=wss://sideimpactor-wisp.yourname.workers.dev
VITE_WISP_URL=
